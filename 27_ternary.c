// wap to cheak given two number are same or different.
#include <stdio.h>
void main()
{
    int a, b;
    printf("enter a value = ");
    scanf("%d", &a);  // 8
    printf("enter b value = ");
    scanf("%d", &b); // 45

    a == b ? printf("nums are same") : printf("nums are different");
}