#include <stdio.h>
void main()
{
    int a, b;
    a = 12;
    b = 5;

    // a += b;
    // a -= b;

    // a += 10;      // a = a + 10;

    // b += a;

    a *= b;

    printf("a = %d\n", a);
    printf("b = %d\n", b);
}